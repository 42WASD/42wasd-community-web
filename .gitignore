# Python / uv
projects/.venv/
__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/

# MkDocs build output
site/

# Bolero spike workspace (not part of the docs repo)
BOLERO-SPIKE/

# .NET build output
bin/
obj/
*.user
*.suo

# Local disposable build output (always inside the repo, never /tmp)
# Deletable anytime: rm -rf .tmp/
.tmp/

# OS / editor
.DS_Store
*.swp

# Scratch design research (not part of the docs/source repo)
design.md

# Generated book-index database for knowledge/distill (rebuild with the script)
knowledge/distill/_index/book-index.db

# Source ebooks (copyrighted, local-only; the distilled guide in docs/knowledge
# is what gets published — never push the raw book text to GitHub)
knowledge/md/
knowledge/src/

# Python bytecode
knowledge/distill/_index/__pycache__/