# Network model

Cilium dependency graphs, default-deny ownership, policy examples, and the
Hubble workflows.

## Subsections

- **Cilium service dependency graph** — logical allow graph.
- **Default-deny ownership** — central vs app repo policy split.
- **Cilium policy example** — illustrative egress CiliumNetworkPolicy.
- **Hubble policy-development workflow** — audit → propose → enforce.
- **Hubble operator queries** — operational recipes.
