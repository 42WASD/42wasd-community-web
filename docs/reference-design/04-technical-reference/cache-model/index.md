# Cache model

Cache candidate rule, key versioning, HybridCache configuration, cross-Pod
invalidation, Dragonfly role, and the SignalR backplane qualification test.

## Subsections

- **Cache candidate rule** — what to cache, what never to cache.
- **Cache key versioning** — namespaced, versioned keys.
- **HybridCache configuration intent** — L1/L2 TTL ranges.
- **Cross-Pod cache invalidation caveat** — short L1 TTL.
- **Dragonfly role** — what it is and is not.
- **SignalR backplane qualification test** — prove before production.
