# Product capabilities

Search architecture, the optional WebRTC boundary, and the mobile path.

## Subsections

- **Search architecture** — PostgreSQL-first, extract only on evidence.
- **Optional WebRTC boundary** — bandwidth-heavy P2P only.
- **Mobile path** — PWA first, MAUI only on demand.
