# GitOps model

Repository ownership, divergence from strict config-repo guidance, third-party
ownership, Argo boundaries, secrets, and Data Protection.

## Subsections

- **GitOps repository ownership** — cluster-infra vs application repos.
- **Difference from strict config-repo GitOps guidance** — accepted tradeoff.
- **Third-party component ownership** — reference, pin, don't copy.
- **Argo AppProject boundaries** — restricted privileges.
- **ApplicationSet role** — controlled generation.
- **Secrets model** — never commit; reference and materialize.
- **ASP.NET Data Protection** — shared key persistence for replicas.
