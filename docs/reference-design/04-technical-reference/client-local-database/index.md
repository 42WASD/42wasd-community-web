# Client local database

The IndexedDB database layout, schema upgrade policy, and app/runtime cache
versioning.

## Subsections

- **Browser IndexedDB database layout** — stores, fields, disposable vs
  precious.
- **IndexedDB schema upgrade policy** — drop/rebuild cache, migrate precious.
- **App/runtime cache versioning** — Service Worker vs IndexedDB version axes.
