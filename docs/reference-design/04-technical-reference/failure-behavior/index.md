# Failure behavior

Required behavior for network failures and external provider failures.

## Subsections

- **Network failure behavior** — per-dependency degradation rules.
- **External provider failure behavior** — never block core content.
