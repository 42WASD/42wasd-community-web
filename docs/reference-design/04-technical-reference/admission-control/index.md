# Admission control

Rate-limit policy classes, backpressure state, response compression policy,
and serialization policy by boundary.

## Subsections

- **Rate-limit policy classes** — named policies and keying.
- **Backpressure state** — saturation signals and degradation order.
- **Response compression policy** — zstd/br/gzip decision tree.
- **Serialization policy by boundary** — the format per wire.
