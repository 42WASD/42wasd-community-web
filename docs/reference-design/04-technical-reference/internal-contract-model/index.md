# Internal contract model

The `.proto` repository layout, protobuf evolution rules, and why backend
services do not share F# types.

## Subsections

- **Internal `.proto` repository layout** — versioned package folders.
- **Protobuf evolution rules** — additive-first, reserve removed numbers.
- **Why not share F# types between backend services** — wire compatibility.
