# Expand SignalR to list/tournament/conversation scopes

Only after the Topic flow is correct (architecture doc Phase 25).

Use group naming/version conventions.

Do not let group names contain unvalidated arbitrary user text.

## Acceptance

```text
[ ] group authorization prevents joining private conversation by guessed ID
[ ] list events are minimal deltas/invalidation
```
